<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.syq.code.mapper.UserMapper">
    <select id="FindByAll" resultMap="userResultMap">
        select * from t_user user left join t_euser euser on user.f_username = euser.f_username
        <where>
            <if test="username !=null and username!=''">
                and user.f_username=#{username}
            </if>
            <if test="age !=null">
                and  user.f_age= #{age}
            </if>
            <if test="phonenumber !=null and phonenumber!=''">
                and user.f_phonenumber=#{phonenumber}
            </if>
            <if test="gender !=null">
                and user.f_gender=#{gender}
            </if>
            <if test="emailaddress !=null and emailaddress!=''">
                and user.f_emailaddress=#{emailaddress}
            </if>
            <if test="password !=null">
                and user.f_password=#{password}
            </if>
            <if test="user.permission !=null and user.permission!=''">
                and euser.f_permission=#{user.permission}
            </if>
        </where>
    </select>
    <delete id="Cancel">
        delete from t_user where f_username=#{username}
    </delete>
    <insert id="Register">
        insert into t_user (f_username, f_age, f_phonenumber, f_gender, f_emailaddress, f_password) VALUES (#{username},#{age},#{phonenumber},#{gender},#{emailaddress},#{emailaddress});
    </insert>
    <select id="FindByUsername" resultMap="userResultMap">
        <if test="username!=null and username!=''">
            select f_username from t_user where f_username = #{username}
        </if>
    </select>
    <select id="FindByLogin" resultMap="userResultMap">
        <if test="username!=null and username!='' and password!=null and password!=''">
            select f_username,f_password from t_user where f_username = #{username} and f_password=#{password}
        </if>
    </select>
    <update id="UpdateByUsername">
        update t_user user left join t_euser euser on user.f_username = euser.f_username
            <set>
                <if test="user.username !=null and user.username!=''">
                    user.f_username=#{user.username},
                </if>
                <if test="user.password !=null and user.password !=''">
                    user.f_password=#{user.password},
                </if>
                <if test="user.age !=null">
                    user.=#{user1.age},
                </if>
                <if test="user.gender !=null">
                    user.f_gender=#{user.gender},
                </if>
                <if test="user.phonenumber !=null and user.phonenumber !=''">
                    user.f_phonenumber=#{user1.phonenumber},
                </if>
                <if test="user.emailaddress !=null and user.emailaddress !=''">
                    user.f_emailaddress=#{user.emailaddress},
                </if>
                <if test="user.permission !=null and user.permission !=''">
                    euser.f_permission=#{user.permission},
                </if>
             </set>
            <where>
                <if test="user.username_old!=null and user.username_old!=''">
                    and user.f_username=#{user.username_old}
                </if>
            </where>
    </update>

    <resultMap id="userResultMap" type="com.syq.code.entity.User">
        <result column="f_id" property="id"></result>
        <result column="f_username" property="username"></result>
        <result column="f_age" property="age"></result>
        <result column="f_phonenumber" property="phonenumber"></result>
        <result column="f_gender" property="gender"></result>
        <result column="f_emailaddress" property="emailaddress"></result>
        <result column="f_password" property="password"></result>
        <result column="f_permission" property="permission"></result>
    </resultMap>
</mapper>